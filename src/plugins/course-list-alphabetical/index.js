/**
 * Default alphabetical sort for the learner dashboard course list.
 *
 * Set slot IDs in `src/shared/constants.js` after checking Ulmo’s
 * frontend-app-learner-dashboard for `PluginSlot` `id` values, then add
 * `PLUGIN_OPERATIONS.Modify` / `Wrap` / etc. here per the Frontend Plugin Framework.
 *
 * Use `PLUGIN_OPERATIONS` / `DIRECT_PLUGIN` / `IFRAME_PLUGIN` from
 * `src/shared/pluginFramework.js` (same package the host uses).
 *
 * @see https://github.com/openedx/frontend-plugin-framework
 */

/** @type {Record<string, object>} */
export const courseListAlphabeticalPluginSlots = {
  // [SLOT_IDS.courseList]: {
  //   keepDefault: true,
  //   plugins: [],
  // },
};
