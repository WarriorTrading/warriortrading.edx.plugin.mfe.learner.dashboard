/**
 * Plugin slot IDs from frontend-app-learner-dashboard.
 * Re-verify when upgrading the learner-dashboard MFE (grep `PluginSlot` / slot READMEs).
 *
 * @see https://github.com/openedx/frontend-app-learner-dashboard/blob/master/src/plugin-slots/CourseListSlot/README.md
 */
export const SLOT_IDS = {
  /** Course list area (enrolled learners only). */
  courseList: 'org.openedx.frontend.learner_dashboard.course_list.v1',
};
