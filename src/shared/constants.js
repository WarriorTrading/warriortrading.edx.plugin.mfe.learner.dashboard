/**
 * Plugin slot IDs from frontend-app-learner-dashboard.
 * Fill these in after grepping the host repo (Ulmo’s pinned revision) for
 * `<PluginSlot` and matching `id` props, then import them from feature plugins.
 */
export const SLOT_IDS = {
  // Example: courseList: 'learner_dashboard_course_list',
};
